{ 
  "name": "familyconnect",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "dependencies": {
    "vite": "^7.3.0"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist -b gh-pages -f"
  }
}
